interface:
  display_name: "Agent-Written Prompt Boundary"
  short_description: "约束 agent 为软件内大模型调用编写的提示词边界"
  default_prompt: "Use $agent-written-prompt-boundary to design or edit the product LLM prompt written during software development, preserving safety constraints and keeping development context out of the runtime prompt."
